<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.embed.mapper.BoardMapper">
	<select id="selectBoardList" resultType="org.embed.dto.BoardDTO">
		<![CDATA[
			SELECT
				board_id, title, hit_cnt, created_datetime
			FROM
				t_board
			WHERE
				deleted_yn = 'N'
			ORDER BY board_id DESC
		]]>
	</select>
	
	<insert id="insertBoard" parameterType="org.embed.dto.BoardDTO"
			useGeneratedKeys="true" keyProperty="boardId">
		<![CDATA[
			INSERT INTO t_board
			(
				title, contents, created_datetime, creator_id
			)
			VALUES
			(
				#{title}, #{contents}, NOW(), 'admin'
			)
		]]>	
	</insert>
	
	<update id="updateHitCount" parameterType="int">
		<![CDATA[
			UPDATE
				t_board
			SET
				hit_cnt = hit_cnt + 1
			WHERE
				board_id = #{boardId}
		]]>
	</update>
	
	<select id="selectBoardDetail" parameterType="int" resultType="org.embed.dto.BoardDTO">
		<![CDATA[
			SELECT
				board_id, title, contents, hit_cnt,
				created_datetime, creator_id
			FROM
				t_board
			WHERE
				board_id = #{boardId} AND deleted_yn = 'N'
		]]>
	</select>
	
	<update id="updateBoard" parameterType="org.embed.dto.BoardDTO">
		<![CDATA[
			UPDATE
				t_board
			SET
				title = #{title},
				contents = #{contents},
				updated_datetime = NOW(),
				updater_id = 'admin'
			WHERE
				board_id = #{boardId}
		]]>
	</update>
	
	<delete id="deleteBoard" parameterType="int">
		<![CDATA[
			DELETE
			FROM
				t_board
			WHERE
				board_id = #{boardId}
		]]>
	</delete>
	
	<insert id="insertBoardFileList" parameterType="org.embed.dto.BoardFileDTO">
		<![CDATA[
			INSERT INTO t_file
			(
				board_id, original_file_name, stored_file_path, 
				file_size, creator_id, created_datetime 
			)
			VALUES
		]]>
		<foreach collection="list" item="item" separator=",">
			(
				#{item.boardId},
				#{item.originalFileName},
				#{item.storedFilePath},
				#{item.fileSize},
				'admin',
				NOW()
			)
		</foreach>	
	</insert>
	
	<select id="selectBoardFileList" 
			parameterType="int" resultType="org.embed.dto.BoardFileDTO">
		<![CDATA[
			SELECT
				id, board_id, original_file_name, 
				FORMAT(ROUND(file_size / 1024), 0) AS file_size 
			FROM
				t_file
			WHERE
				board_id = #{boardId} AND deleted_yn = 'N'
		]]>
	</select>
	
	<select id="selectBoardFileInformation" 
			parameterType="map" resultType="org.embed.dto.BoardFileDTO">
		<![CDATA[
			SELECT
				original_file_name,
				stored_file_path, 
				file_size 
			FROM
				t_file
			WHERE
				id = #{id} AND board_id = #{boardId} AND deleted_yn = 'N'
		]]>
	</select>
	
</mapper>
